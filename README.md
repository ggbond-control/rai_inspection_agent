# RAI Inspection Agent

Inspection-specific RAI agent project. Business tools live here instead of in
the generic RAI framework.

ROS 2 must be sourced before running the app or tests that import ROS packages:

```bash
source /opt/ros/jazzy/setup.bash
source /home/jazzy/task_ws/install/setup.bash
uv run pytest
```
